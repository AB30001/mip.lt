{
  "name": "mip-lt",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/generate-speedtest-payload.mjs",
    "preview": "wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "deploy:agent": "wrangler deploy --config workers/news-agent/wrangler.jsonc",
    "cf-typegen": "wrangler types",
    "cf-typegen:agent": "wrangler types --config workers/news-agent/wrangler.jsonc workers/news-agent/worker-configuration.d.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.7.0",
    "@astrojs/rss": "^4.0.0",
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^6.4.8",
    "tailwindcss": "^4.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "wrangler": "^4.83.0"
  },
  "overrides": {
    "vite": "^7.3.2"
  }
}
